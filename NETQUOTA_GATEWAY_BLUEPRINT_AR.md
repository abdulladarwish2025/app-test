# مخطط تطبيق NetQuota Gateway

> نظام احترافي لتحديد استهلاك يومي مستقل لكل جهاز على الشبكة، من غير تثبيت أي تطبيق على أجهزة المستخدمين، ويستمر في العمل حتى لو موبايل أو كمبيوتر الإدارة مقفول.

## 1. الفكرة في سطر واحد

تطبيق الإدارة لا يقطع الإنترنت بنفسه. القطع والحساب يتمان داخل **جهاز Gateway صغير دائم التشغيل** يوضع بين الإنترنت وكل أجهزة المنزل أو المكان، بينما يستخدم صاحب الشبكة تطبيق موبايل أو لوحة ويب للتحكم فقط.

## 2. الحقيقة التقنية المهمة

لا يمكن لتطبيق Android وحده أن يفرض حصة على أجهزة أخرى إذا:

- الراوتر لا يدعم الحصص.
- لا نريد تثبيت برنامج على كل جهاز.
- موبايل أو كمبيوتر الإدارة قد يكون مغلقًا.

لا بد أن تمر كل حركة الإنترنت عبر نقطة تحكم تعمل دائمًا. الحل العملي هو واحد من الآتي:

1. راوتر يدعم OpenWrt ويحل محل الراوتر الحالي.
2. جهاز Raspberry Pi أو Mini PC بواجهتي شبكة يعمل كـ Gateway.
3. جهاز Gateway مخصص مع Access Point منفصل.

الحل الموصى به: **NetQuota Box** صغير يعمل بنظام Linux/OpenWrt، مع تطبيق **NetQuota Admin** للموبايل.

## 3. شكل التوصيل الصحيح

```text
الإنترنت / ONT
      │
      ▼
NetQuota Box (الحساب + تطبيق القواعد + قاعدة البيانات)
      │
      ▼
Access Point أو الراوتر الحالي في وضع AP
      │
      ├── هاتف الأب
      ├── هاتف الطفل
      ├── التلفزيون
      ├── اللابتوب
      └── أي جهاز Wi-Fi أو كابل
```

### شرط مهم

يجب إلغاء DHCP وNAT في الراوتر القديم وتشغيله في وضع **Access Point / Bridge**، أو استخدام Access Point خلف الـ Gateway. إذا ظل الراوتر القديم يعمل كراوتر عادي خلف الـ Gateway، فسيرى النظام الراوتر كجهاز واحد ولن يستطيع حساب كل عميل منفردًا.

إذا كان راوتر الشركة لا يدعم Bridge، يظل متصلًا بالإنترنت كما هو، ثم يوصل NetQuota Box بعده، وتتصل كل أجهزة المستخدمين بالـ Wi-Fi الصادر من NetQuota Box أو Access Point خلفه. وجود Double NAT مقبول في النسخة الأولى، مع توفير إعدادات خاصة للألعاب وفتح المنافذ عند الحاجة.

## 4. اسم وهوية المنتج

- اسم النظام: **NetQuota Gateway**
- اسم جهاز التحكم: **NetQuota Box**
- اسم تطبيق الإدارة: **NetQuota Admin**
- الوعد الرئيسي: **كل جهاز له حصته، والشبكة تحت سيطرتك دائمًا.**

## 5. الوظائف الأساسية

### إدارة الحصص

- تحديد حصة يومية لكل جهاز: مثل 1 GB أو 5 GB.
- حصة أسبوعية أو شهرية اختيارية.
- حساب التحميل والرفع معًا افتراضيًا، مع إمكانية فصل كل منهما.
- تجديد تلقائي عند منتصف الليل حسب المنطقة الزمنية المحددة.
- إضافة باقة مؤقتة أو Bonus من لوحة الإدارة.
- ترحيل المتبقي لليوم التالي كخيار يمكن تفعيله لكل مستخدم.
- سياسات عند انتهاء الحصة:
  - قطع كامل.
  - سرعة طوارئ منخفضة.
  - السماح بتطبيقات أو مواقع محددة فقط.
  - طلب موافقة المسؤول لإضافة رصيد.

### التحكم الفوري

- إيقاف أو تشغيل الإنترنت لأي جهاز بضغطة واحدة.
- تحديد سرعة تحميل ورفع لكل جهاز.
- إنشاء جدول نوم أو دراسة أو عمل.
- إيقاف جميع الأجهزة غير المعروفة تلقائيًا.
- وضع ضيف بحصة ومدة انتهاء تلقائية.
- تجميع الأجهزة تحت شخص واحد؛ مثل هاتف ولابتوب وتلفزيون الطفل في حصة مشتركة.

### المراقبة والتقارير

- الاستهلاك الحالي والمتبقي لكل جهاز.
- سرعة لحظية وتحميل ورفع منفصلان.
- أكثر الأجهزة استهلاكًا.
- رسم استهلاك بالساعة واليوم والشهر.
- سجل واضح: من غيّر الحصة، ومتى تم القطع أو الفتح، وسبب القرار.
- إشعارات عند 50% و80% و100% من الحصة.
- تنبيه عند دخول جهاز جديد أو محاولة تغيير الهوية.

### اكتشاف الأجهزة

- التعرف من MAC Address وDHCP lease واسم الجهاز وخصائص الشبكة.
- إعطاء اسم وصورة ونوع ومالك لكل جهاز.
- دعم Private/Random MAC؛ العنوان العشوائي يكون غالبًا ثابتًا لنفس شبكة Wi-Fi، ويمكن إعادة ربط الجهاز بالمالك عند تغييره.
- بوابة تعريف اختيارية لأول اتصال لكتابة اسم المستخدم أو إدخال PIN.

## 6. تجربة الاستخدام

### الإعداد لأول مرة

1. يوصل المستخدم NetQuota Box بين مصدر الإنترنت والـ Access Point.
2. يفتح تطبيق NetQuota Admin ويتصل بالجهاز عبر QR Code مطبوع عليه.
3. يفحص النظام الشبكة ويعرض الأجهزة المتصلة.
4. يسمي المستخدم الأجهزة ويختار أصحابها.
5. يحدد حصة افتراضية، مثل 2 GB يوميًا، ثم يعدل الاستثناءات.
6. تبدأ القواعد فورًا وتظل محفوظة وتعمل محليًا من دون إنترنت سحابي.

### الشاشة الرئيسية

- حالة الإنترنت والـ Gateway.
- إجمالي استهلاك الشبكة اليوم.
- بطاقات الأشخاص والأجهزة مع دائرة توضح النسبة المستهلكة.
- أزرار سريعة: إيقاف، إضافة 500 MB، تمديد ساعة، تحديد السرعة.
- قائمة تنبيهات مختصرة.

### شاشة الجهاز

- الاسم والنوع والمالك وIP وMAC.
- المستخدم والمتبقي ووقت التجديد.
- السرعة اللحظية.
- تعديل الحصة والسرعة والجدول.
- سجل آخر 30 يومًا.
- زر حظر فوري وزر سماح مؤقت.

### شاشة السياسات

- قوالب: طفل، ضيف، عمل، تلفزيون، جهاز ألعاب.
- حصة وجدول وسرعة وتصرف عند انتهاء الحصة.
- تطبيق القالب على جهاز أو شخص أو مجموعة.

## 7. المعمارية التقنية المقترحة

```text
┌──────────────── NetQuota Admin ────────────────┐
│ Android / iOS / Web PWA                        │
│ لوحة تحكم، تنبيهات، تقارير، إعداد السياسات     │
└──────────────────────┬─────────────────────────┘
                       │ HTTPS محلي أو Remote Relay اختياري
┌──────────────────────▼─────────────────────────┐
│                 NetQuota Core                  │
│ REST API + Policy Engine + Device Registry     │
│ Quota Scheduler + Audit Log + Notifications    │
├────────────────────────────────────────────────┤
│ SQLite WAL                                     │
│ الأجهزة، الحصص، العدادات، السياسات، السجل       │
├────────────────────────────────────────────────┤
│ Linux/OpenWrt Enforcement                      │
│ nftables: حساب وقطع                            │
│ tc/CAKE: تحديد السرعات                         │
│ DHCP/DNS: تثبيت الهوية وسياسات المواقع         │
└──────────────────────┬─────────────────────────┘
                       │ كل حركة الشبكة
                  أجهزة المستخدمين
```

### محرك الحساب والقطع

- يعدّ `nftables` البايتات لكل IP قبل تنفيذ NAT، في اتجاهي الرفع والتحميل.
- يربط Device Registry عنوان IP بعنوان MAC من DHCP leases.
- يمنح كل جهاز حجز DHCP ثابتًا داخليًا لتجنب اختلاط العدادات.
- يجمع Quota Engine الفروق من العدادات بدل إعادة جمع الإجمالي، لمنع التكرار.
- عند الوصول للحد، يضيف الجهاز فورًا إلى مجموعة حظر ديناميكية داخل `nftables`.
- يستخدم `tc` مع CAKE أو HTB للحد من السرعة بدل القطع الكامل.
- تعمل قواعد IPv4 وIPv6 معًا حتى لا يتجاوز جهاز الحظر عبر IPv6.

### التخزين وتحمل انقطاع الكهرباء

- قاعدة SQLite تعمل بنمط WAL مع معاملات ذرية.
- حفظ العدادات كل عدة ثوانٍ، وكذلك عند تجاوز مقدار بيانات محدد، لتحقيق توازن بين الدقة وعمر وحدة التخزين.
- يحتفظ النظام بآخر عداد Kernel وآخر إجمالي مؤكد؛ بعد إعادة التشغيل يبدأ Session جديدة من دون احتساب البايتات مرتين.
- تعمل الخدمة تلقائيًا عند الإقلاع عبر `procd` في OpenWrt أو `systemd` في Linux.
- القواعد والحصص موجودة داخل الـ Gateway نفسه؛ لا يعتمد القطع على موبايل الإدارة أو السحابة.
- ساعة الجهاز تتزامن عبر NTP، مع حارس يمنع تصفير الحصة مرتين إذا تغيرت الساعة.
- UPS صغير اختياري يحمي من الفصل المتكرر، لكنه ليس شرطًا للتشغيل بعد عودة الكهرباء.

قد تضيع فقط كمية صغيرة جدًا تم استهلاكها بين آخر حفظ ولحظة فصل الكهرباء المفاجئ. يمكن جعل هامش الفقد ثواني قليلة، ولا يوجد نظام تخزين عادي يضمن صفر فقد مطلق بلا بطارية أو تخزين دائم لكل Packet.

## 8. التحكم من خارج المنزل

النسخة الأساسية تعمل داخل الشبكة بالكامل. للتحكم عن بعد يمكن إضافة خدمة Cloud Relay اختيارية:

- الـ Gateway ينشئ اتصالًا مشفرًا صادرًا إلى الخدمة؛ لا نفتح لوحة الإدارة مباشرة على الإنترنت.
- التطبيق يرى الحالة ويرسل أوامر موقعة عبر القناة المشفرة.
- عند توقف السحابة، تستمر كل الحصص والجداول والقطع محليًا.
- لا تمر حركة الإنترنت الخاصة بالمستخدمين عبر السحابة؛ تمر فقط بيانات الإدارة والملخصات.
- يمكن تقديم وضع Local Only لمن يريد أعلى خصوصية.

## 9. نموذج البيانات الأساسي

```text
Device
  id, mac, ip, hostname, displayName, ownerId, status, firstSeen, lastSeen

Owner
  id, name, avatar, sharedQuotaPolicyId

QuotaPolicy
  id, period, limitBytes, rollover, resetTime, onExhausted, throttleRate

UsageBucket
  deviceId, periodStart, downloadBytes, uploadBytes, updatedAt, revision

Schedule
  targetId, days, startTime, endTime, action

AuditEvent
  actor, target, action, reason, createdAt, oldValue, newValue
```

## 10. API داخلية مقترحة

```http
GET    /api/v1/devices
GET    /api/v1/devices/{id}/usage
PUT    /api/v1/devices/{id}/quota
POST   /api/v1/devices/{id}/pause
POST   /api/v1/devices/{id}/resume
POST   /api/v1/devices/{id}/bonus
PUT    /api/v1/devices/{id}/speed-limit
GET    /api/v1/reports/daily
GET    /api/v1/events/stream
```

مثال ضبط حصة:

```json
{
  "period": "daily",
  "limitBytes": 5368709120,
  "resetTime": "00:00",
  "timeZone": "Africa/Cairo",
  "onExhausted": "throttle",
  "throttleDownKbps": 128,
  "throttleUpKbps": 64
}
```

## 11. الأمان

- ربط أول مرة بواسطة QR يحتوي Secret فريد، مع تغيير المفتاح بعد الاقتران.
- HTTPS بشهادة محلية موثوقة داخل التطبيق.
- كلمات المرور والمفاتيح لا تخزن كنص صريح.
- صلاحيات: مالك، مدير، ومشاهد فقط.
- جميع أوامر الحظر وتعديل الحصص تسجل في Audit Log.
- تحديثات Firmware موقعة رقميًا مع إمكانية الرجوع للإصدار السابق.
- حماية واجهة الإدارة من Brute Force وCSRF وطلبات الشبكات غير الموثوقة.
- لا يعتمد التعرف على IP وحده، ولا يثق في اسم الجهاز المرسل من العميل.
- شبكة إدارة منفصلة اختيارية ومنع أجهزة الضيوف من الوصول إلى واجهة الـ Gateway.

## 12. حالات يجب التعامل معها

- تغيير IP أو إعادة تشغيل الجهاز.
- MAC عشوائي أو تغييره يدويًا.
- اتصال جهاز عبر IPv6 فقط.
- إعادة تشغيل الـ Gateway في منتصف اليوم.
- تغير التوقيت الصيفي أو فشل NTP.
- جهاز غير معروف يدخل الشبكة.
- وصول جهاز إلى الحصة أثناء Download كبير.
- نقل جهاز من Wi-Fi إلى كابل.
- انقطاع السحابة أو الإنترنت مع بقاء الشبكة المحلية.
- محاولة استخدام DNS خارجي أو VPN؛ الـ VPN لا يتجاوز حصة البايتات لأن الحساب يتم على كل الحركة، بصرف النظر عن محتواها.

## 13. ما لن نعتمد عليه

- **ARP Spoofing من كمبيوتر:** غير مستقر، يتوقف عند إغلاق الكمبيوتر، وقد يسبب مشاكل للشبكة.
- **تطبيق إدارة فقط:** لا يملك موقعًا داخل مسار البيانات، لذلك لا يستطيع فرض القطع.
- **حساب من الراوتر القديم عبر صفحة الإدارة:** غير ممكن إذا لم يوفر الراوتر عدادات وقواعد مناسبة أو API موثوقًا.
- **سيرفر سحابي وحده:** لا يرى حركة المنزل ولا يستطيع منعها إلا إذا مر كل الإنترنت خلال VPN، وهذا ما يزال يحتاج جهاز Gateway دائمًا داخل المنزل.
- **الاعتماد على MAC فقط:** قابل للتغيير؛ يجب دمجه مع DHCP وسجل الجهاز وربط المستخدم.

## 14. خطة التنفيذ

### المرحلة الأولى: نسخة عملية MVP

- Gateway يعمل على Linux أو OpenWrt.
- اكتشاف الأجهزة وتثبيت DHCP leases.
- عد تحميل ورفع لكل جهاز.
- حصة يومية وتجديد منتصف الليل.
- قطع وفتح فوري مع دعم IPv4 وIPv6.
- لوحة ويب Responsive تعمل من الموبايل.
- حفظ واسترجاع صحيح بعد إعادة التشغيل.

### المرحلة الثانية: منتج منزلي احترافي

- تطبيق Android مستقل.
- مجموعات وأشخاص وحصص مشتركة.
- جداول وحدود سرعة وBonus.
- تقارير وإشعارات واكتشاف جهاز جديد.
- نسخ احتياطي مشفر للإعدادات.

### المرحلة الثالثة: تحكم عن بعد ومنتج تجاري

- Cloud Relay اختياري.
- حسابات متعددة وصلاحيات.
- Firmware updates موقعة.
- إدارة أكثر من موقع أو فرع.
- اشتراك اختياري للتقارير السحابية، مع بقاء التحكم الأساسي محليًا.

## 15. معايير قبول النسخة الأولى

- ظهور جهاز جديد خلال 10 ثوانٍ من اتصاله.
- دقة الحساب لا تقل عن 99% في اختبار نقل بيانات معروف الحجم.
- قطع الجهاز خلال ثانيتين من بلوغ الحصة.
- عدم تأثر باقي الأجهزة عند قطع جهاز واحد.
- استمرار القواعد إذا أغلق تطبيق الإدارة أو الهاتف.
- استعادة الحصص والعدادات تلقائيًا بعد فصل الكهرباء وعودتها.
- عدم تجاوز الحظر عبر IPv6 أو تغيير DNS أو تشغيل VPN.
- تصفير يومي مرة واحدة فقط في التوقيت الصحيح.
- جميع تغييرات الإدارة مسجلة وقابلة للمراجعة.

## 16. قرار المنتج النهائي

هذا المشروع ليس بديلًا شكليًا للتطبيق الحالي، بل معمارية مختلفة بالكامل:

- المشروع الحالي: Agent يثبت على كل Android ويقطع الإنترنت محليًا.
- المشروع الجديد: Gateway مركزي يرى كل الأجهزة ويطبق الحصص عليها من الشبكة.

أفضل بداية عملية هي بناء **NetQuota Core + لوحة ويب** على Mini PC أو جهاز OpenWrt بواجهتي شبكة. بعد إثبات دقة الحساب والقطع واستعادة الحالة بعد الكهرباء، يبنى تطبيق Android كواجهة إدارة جميلة فوق نفس الـ API.

## 17. بداية التنفيذ الموجودة في المستودع

- `gateway-admin/`: تطبيق Android مستقل باسم حزمة `com.netquota.gateway.admin`، يعمل بوضع Demo ويتصل بالـGateway API.
- `gateway-core/`: محرك Node.js محلي مع SQLite وحصص يومية وحفظ عدادات وAPI ووضع محاكاة آمن.
- `.github/workflows/gateway-admin-apk.yml`: اختبار المحرك والتطبيق وبناء APK قابل للتثبيت ورفعه ضمن GitHub Artifacts.
